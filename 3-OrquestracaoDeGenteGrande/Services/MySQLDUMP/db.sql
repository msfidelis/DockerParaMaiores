CREATE DATABASE IF NOT EXISTS superlogica;
USE superlogica;

CREATE TABLE IF NOT EXISTS tabelinha (
	ID PRIMARY KEY AUTO_INCREMENT, 
	DES VARCHAR(10)
);

INSERT INTO tabelinha ('DES') VALUES ('PERDEU'), ('GANHOU'), ('PERDEU'), ('GANHOU'), ('PERDEU'), ('GANHOU');